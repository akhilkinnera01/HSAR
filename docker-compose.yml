version: '3.8'

services:
  proxy:
    build:
      context: .
      dockerfile: deploy/Dockerfile.proxy
    ports:
      - "8080:8080"
    environment:
      - SIGNAL_ENGINE_TARGET=signal-engine:50051
      - UPSTREAM_BASE_URL=http://backend:8081
      - TENANT_API_KEYS=dev-key-1:default-tenant
      - TENANT_RATE_RPS=100
      - TENANT_RATE_BURST=100
      - MODE=${MODE:-shadow}
      - CANARY_PCT=${CANARY_PCT:-0}
      - ENFORCE_KILL_SWITCH=${ENFORCE_KILL_SWITCH:-false}
      - PORT=8080
      - METRICS_ENABLED=true
      - OTEL_EXPORTER_OTLP_ENDPOINT=${OTEL_EXPORTER_OTLP_ENDPOINT:-otel-collector:4317}
    depends_on:
      - signal-engine
      - backend
    networks:
      - hsar-net

  signal-engine:
    build:
      context: .
      dockerfile: deploy/Dockerfile.signal
    expose:
      - "50051"
    networks:
      - hsar-net

  backend:
    image: python:3.10-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: ["python3", "deploy/echo_backend.py"]
    ports:
      - "8081:8081"
    expose:
      - "8081"
    networks:
      - hsar-net

  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    networks:
      - hsar-net
    profiles:
      - ollama

networks:
  hsar-net:
    driver: bridge

volumes:
  ollama-data: